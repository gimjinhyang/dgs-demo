type Holiday {
    year: Int
    month: Int
    dayList : [HolidayDay]
}

type HolidayMonth {
    year: Int
    month: Int
    dayList : [HolidayDay]
}

type HolidayDay {
    day: Int
    name: String
}